# تقرير الفحص والإصلاح — LF-IIS

## الملخص التنفيذي

تم فحص مشروع **LF-IIS** على مستوى البنية، المصادقة، تحميل بيانات لوحة التحكم، واتساق واجهة التحليلات. السبب الأوضح للبطء في تسجيل الدخول كان وجود استدعاء `bootstrapOwner` قبل كل محاولة `signInWithPassword`؛ أي أن النظام كان ينتظر دورة شبكية إضافية مخصصة لتهيئة مالك النظام حتى عندما يكون الحساب موجودًا بالفعل. أزيل هذا الاستدعاء من مسار الدخول، كما أزيل endpoint غير المستخدم الذي كان يسمح بإنشاء Owner من بيانات الدخول نفسها، لأن ذلك يمثل سلوكًا غير مناسب أمنيًا في شاشة الدخول.

تم كذلك توحيد مؤشرات التحليلات مع مكوّن `StatCard` واحد، وإضافة نظام CSS مشترك للبطاقات ولوحات الرسوم والشبكات المتجاوبة، مع تحسين شاشة الدخول بإضافة حالة التحقق الأولي، ومنع النقر المتكرر أثناء الطلب، وإظهار/إخفاء كلمة المرور، واستخدام `email` و`autocomplete` الصحيحين. كما تم إصلاح اتجاه مستند HTML الجذري إلى العربية من اليمين إلى اليسار، وإضافة شاشة إعداد ودّية عند غياب متغيرات Supabase بدل انهيار SSR بخطأ `supabaseUrl is required`.

## النتائج الأساسية قبل الإصلاح

| المجال | النتيجة قبل الإصلاح | الأثر |
|---|---|---|
| تسجيل الدخول | `handleLogin` يستدعي `bootstrapOwner` ثم `signInWithPassword` | طلب شبكة إضافي في كل محاولة وزمن انتظار غير ضروري |
| تهيئة الجلسة | عدم وجود حالة واضحة للتحقق من الجلسة عند بدء التطبيق | احتمال وميض شاشة الدخول وتأخر الإحساس بالاستقرار |
| بطاقات التحليلات | بطاقات Dashboard تستخدم `StatCard`، بينما بطاقات الميزانية والتقارير تستخدم inline styles مستقلة | اختلاف في الارتفاع، المسافات، الحواف، الظلال والاستجابة |
| شبكات التقارير | استخدام `repeat(4,1fr)` و`repeat(3,1fr)` داخل كتل مخصّصة | ازدحام محتمل على الشاشات الصغيرة |
| إعداد Supabase | `createClient` يُنشأ بقيم undefined عند غياب البيئة | فشل SSR قبل عرض التطبيق |
| اللغة والاتجاه | `RootShell` كان يستخدم `lang="en"` دون `dir="rtl"` | تكامل عربي غير مكتمل على مستوى المستند |

## الإصلاحات المنفذة

### تسريع المصادقة وتحسين الأمان

أصبح مسار الدخول يرسل طلبًا واحدًا مباشرًا إلى `supabase.auth.signInWithPassword`. أضيفت حماية `isSigningIn` لمنع النقر المتكرر، وحالة `authChecking` لعرض شاشة تهيئة صغيرة أثناء استعادة الجلسة. بعد نجاح المصادقة، يستمر تحميل الدور والملف التعريفي في طلبين متوازيين فقط كما كان مقصودًا، مع معالجة أخطاء صريحة بدل ترك الوعود المرفوضة دون معالجة.

أزيل `bootstrapOwner` من `src/lib/admin-users.functions.ts` ومن استيراداته. إنشاء الحسابات أصبح محصورًا في دالة إنشاء المدير التي تُستدعى من شاشة المستخدمين بعد المصادقة. هذا يمنع تحويل أول محاولة دخول إلى مسار إنشاء Owner ذي صلاحيات عالية.

### توحيد بطاقات التحليلات

أصبحت مؤشرات الميزانية ومؤشرات التقارير الإدارية تستخدم `StatCard` نفسه المستخدم في Dashboard. تم نقل الاختلاف إلى لوحة ألوان موحدة بدل تكرار هندسة البطاقة في عدة كتل inline. أضيفت أصناف `analytics-stat-card` و`analytics-kpi-grid` و`analytics-panel` و`analytics-chart-grid` و`analytics-chart-card`، مع نقاط توقف للشاشات المتوسطة والصغيرة.

النتيجة أن البطاقات أصبحت تشترك في الحد الأدنى للارتفاع، الحشو، نصف قطر الحواف، التظليل، حالة hover، التفاف النص، والانتقال إلى عمود واحد على الشاشات الصغيرة. كما أزيلت الرموز التعبيرية من عنوان لوحة التقارير الإدارية حتى تبقى العناوين أكثر اتساقًا مع لغة النظام البصرية.

### معالجة بيئة التشغيل والتكامل

أصبحت طبقة Supabase تستخدم client آمنًا قابلًا للإنشاء حتى عند غياب المتغيرات، مع `isSupabaseConfigured` وشاشة إعداد عربية واضحة. هذا لا يفعّل البيانات دون إعداد صحيح، لكنه يمنع انهيار SSR ويشرح للمشغّل المتغيرات المطلوبة. أضيف `.env.example` يحوي متغيرات الواجهة ومتغيرات الخادم، مع تنبيه صريح إلى أن مفتاح الخدمة سري ولا يوضع في الواجهة.

تم تحديث `<html>` في `src/routes/__root.tsx` إلى `lang="ar" dir="rtl"`، كما استبدلت دوال الخادم صيغة `inputValidator` القديمة بصيغة `validator` الحالية، ما أزال تحذيرات API أثناء البناء.

## الملفات المعدّلة

| الملف | نطاق التغيير |
|---|---|
| `src/App.tsx` | المصادقة، حالات التحميل، نموذج الدخول، توحيد بطاقات التحليلات، شبكات التقارير |
| `src/styles.css` | tokens وأصناف موحدة للبطاقات واللوحات والاستجابة وحالات الدخول |
| `src/integrations/supabase/client.ts` | حماية غياب إعدادات Supabase وشاشة الإعداد البديلة |
| `src/lib/admin-users.functions.ts` | إزالة bootstrap غير الآمن وتحديث validator |
| `src/routes/__root.tsx` | اللغة والاتجاه العربيان للمستند الجذري |
| `.env.example` | نموذج إعداد قابل للنسخ والنشر |

## التحقق والاختبارات

| الاختبار | النتيجة |
|---|---|
| `tsc --noEmit` بعد الإصلاحات | ناجح، دون أخطاء TypeScript |
| `vite build` بعد الإصلاحات | ناجح في مراحل client وSSR وNitro |
| التشغيل دون متغيرات Supabase | ناجح؛ ظهرت شاشة إعداد عربية بدل خطأ SSR |
| إعادة تحميل شاشة الدخول المهيأة | ناجح؛ placeholder أصبح «البريد الإلكتروني» |
| إظهار وإخفاء كلمة المرور | ناجح؛ النوع والـ label يتغيران كما هو متوقع |
| محاولة دخول غير صالحة | ناجحة؛ ظهرت رسالة عربية وعاد زر الدخول من حالة الطلب |
| تحذيرات `inputValidator` | أزيلت |
| تحذير حجم chunks | ما زال موجودًا بسبب اعتماديات `xlsx` و`recharts` ومسار التطبيق الكبير، لكنه تحذير أداء وليس فشل بناء |

## ملاحظات النشر

يجب نسخ `.env.example` إلى `.env.local` أو إعداد متغيرات البيئة في منصة النشر، ثم ضبط `VITE_SUPABASE_URL` و`VITE_SUPABASE_PUBLISHABLE_KEY` للواجهة، وضبط `SUPABASE_URL` و`SUPABASE_SERVICE_ROLE_KEY` للوظائف الخادمية فقط. لا يجب وضع `SUPABASE_SERVICE_ROLE_KEY` في أي ملف يبدأ بـ `VITE_` أو في كود المتصفح.

ظل تحذير حجم الحزم قائمًا لأن التطبيق يحتوي على `xlsx` و`recharts` ومسار إداري كبير في حزمة واحدة. تم تحسين زمن الدخول نفسه بإزالة طلب bootstrap، بينما يمكن تنفيذ مرحلة لاحقة من code-splitting عبر تحميل تبويبات التقارير والمكتبات الثقيلة عند الطلب إذا كان الهدف خفض زمن التحميل الأولي للواجهة أيضًا.

## خلاصة التسليم

النسخة الموجودة في الأرشيف المرفق هي نسخة المصدر المعدّلة، وتحتوي التقرير الحالي وملف `.env.example`. تم اختبار المسار المتاح دون حساب حقيقي باستخدام بيانات دخول تجريبية غير صالحة فقط؛ لم يتم استخدام أو طلب بيانات اعتماد شخصية، ولذلك يظل اختبار الدخول الناجح إلى قاعدة البيانات الفعلية خطوة مطلوبة على بيئة النشر بعد ضبط متغيرات Supabase.
